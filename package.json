{
  "name": "bean-counter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify main.js -o bundle.js -t [ babelify --presets [ es2015 ] --plugins [ add-module-exports ] ]",
    "start": "npm run build && node server.js"
  },
  "author": "Connor Smallman",
  "license": "ISC",
  "dependencies": {
    "hyperapp": "^0.4.2",
    "hyperx": "^2.0.5"
  },
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0"
  }
}
